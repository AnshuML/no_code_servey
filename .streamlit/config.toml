# Streamlit theme — run app from project root so this file is picked up.

[theme]
base = "dark"
primaryColor = "#2dd4bf"
backgroundColor = "#0b1220"
secondaryBackgroundColor = "#111827"
textColor = "#e2e8f0"
font = "sans serif"

[browser]
gatherUsageStats = false
